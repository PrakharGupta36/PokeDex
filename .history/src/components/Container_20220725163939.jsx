export default function Container({ data }) {
  return (
    <div className='container'>
      {data.map((e, i) => {
        const { name } = e;
        return (
          
        );
      })}
    </div>
  );
}
